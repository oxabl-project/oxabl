[package]
name = "oxabl_ast"
version = "0.4.0"
edition = "2024"
license = "MIT"
description = "AST node definitions for the Oxabl ABL parser"
repository = "https://github.com/oxabl-project/oxabl/crates/oxabl_ast"

[dependencies]
rust_decimal = "1.40.0"
smallvec = { version = "1", features = ["union"] }